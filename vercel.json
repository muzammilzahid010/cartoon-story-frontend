{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "ignoreCommand": "if [ -d server ]; then rm -rf server; fi",
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
